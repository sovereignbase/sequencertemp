# Overlapping mask gate

Two replicas mask the same two-frame origin while concurrent content is added
inside and after that origin. The signed prefix can become negative, but the
local gate points to the head zero-reservation. Nothing can be inserted to the
left of `head`, so its cached projected position remains zero in every delivery
order.
